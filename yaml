services:
  - type: web
    name: brain-bender-daily
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
    healthCheckPath: /health
    autoDeploy: true